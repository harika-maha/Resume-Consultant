version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    environment:
      - VITE_APP_API_URL=http://localhost:8000